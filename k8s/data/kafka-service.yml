apiVersion: v1
kind: Service
metadata:
  name: sports-kafka
  namespace: sports-app
spec:
  selector:
    app: sports-kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
  type: ClusterIP